// Package core provides the built-in MOCA framework doctypes
// including User, Role, DocType, and other system-level definitions.
//
// This package is always installed and is composed into the main binary
// via the Go workspace (go.work). It serves as the foundation app module
// that every MOCA deployment includes.
package core
